let numbers :number[]=[3,4,6];
console.log(numbers)
const str:string[]=["king","queen","rem"]
console.log(str)

const numlist:number[]=[];
numlist.push(10);
numlist.push(100);
numlist.push(20);
numlist.push(50);
numlist.push(6);
console.log(numlist)
numlist.pop();

// Array functions
// ->push,pop
// 🎯 Array Functions – One Look Revision

// 🟢 map → change data
// 🟢 filter → select data
// 🟢 reduce → single value
// 🟢 forEach → loop only
// 🟢 find → one element
// 🟢 includes → true / false